# Shell scripts must stay LF or bash fails on Windows checkouts ("\r: command not found")
*.sh text eol=lf

# Keep manifests/templates consistent across platforms
*.json text eol=lf
*.asmdef text eol=lf
*.md text eol=lf
