# Examples (planned — v1.0)

A primitive-only Unity 6 example project will live here:

```text
Capsule = Player
Cube    = Enemy
Plane   = Ground
Sphere  = Pickup
```

No external assets, no Asset Store content — so the example is fully
redistributable under MIT and runs from a fresh Unity 6 install.
